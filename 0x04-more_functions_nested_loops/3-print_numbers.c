# include "main.h"

/**
 * 3-print_numbers - check if number 0 - 9 prints
 * @c: char to be checked
 *
 * Return: 1
 */

void print_numbers(void)
{
	if (c >= '0' && c <= '9')
		return (1);
	else
		return ('\n');
}
